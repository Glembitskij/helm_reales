---
extraEnvs:
  - name: "ELASTICSEARCH_USERNAME"
    value: elastic
  - name: "ELASTICSEARCH_PASSWORD"
    valueFrom:
      secretKeyRef:
        name: elasticsearch-es-elastic-user
        key: elastic
apmConfig:
  apm-server.yml: |
    apm-server:
      host: "0.0.0.0:8200"

    queue: {}

    output.elasticsearch:
      hosts: ["https://elasticsearch-es-masters:9200"]
      username: "${ELASTICSEARCH_USERNAME}"
      password: "${ELASTICSEARCH_PASSWORD}"
      ssl.verification_mode: none

# apm-server:
#   spec:
#   version: 7.15.0
#   count: 1
#   elasticsearchRef:
#     name: elasticsearch
#   kibanaRef:
#     name: elasticsearch
